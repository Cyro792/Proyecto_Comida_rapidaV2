server:
  port: 8080 # Puerto de entrada principal

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # Rutas hacia el microservicio de catalogo
        - id: ms-catalogo
          uri: http://localhost:8082
          predicates:
            - Path=/api/productos/**